import org.w3c.dom.ls.LSOutput;

public class isOldEnoughToDrink {
    public boolean isOldEnoughToDrink(int age) {
        if (age>=18){
            return true;
        }
        else return false;
    }
    System.out.println(boolean);
}
